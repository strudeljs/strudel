import { rollup } from 'rollup';
import uglify from 'rollup-plugin-uglify';
import { minify } from 'uglify-js';

export default {
  entry: 'src/main.js',
  dest: 'build/quantum.min.js',
  plugins: [
    uglify({}, minify)
  ]
}
